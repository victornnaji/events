import React from 'react';
import Menu from '../../Components/Menu/Menu';

const Navbar = () => {
    return (
        <div>
            <Menu />
        </div>
    )
}

export default Navbar;
